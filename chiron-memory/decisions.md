# decision

A choice made and the reasoning behind it — the path taken over the alternatives.

## Chose Vite + React 19 + TypeScript + react-router-dom + Vitest, styled with plain CSS cus…

What: Chose Vite + React 19 + TypeScript + react-router-dom + Vitest, styled with plain CSS custom properties instead of a UI framework · Why: plain CSS makes the light/dark theme a single token swap, and avoids fighting a component library when building the bracket's custom layout later · Where: package.json, vite.config.ts, src/styles/tokens.css <!-- id: 40bace98-9a0b-4921-86a5-210880b65659-0 -->
